# cargo-deny configuration — dependency license compatibility gate (#78).
# Scope: the root cargo workspace (crates/*). Run: cargo deny check licenses

[licenses]
version = 2
# Permissive licenses acceptable for dependencies of the AGPL-3.0-only
# lasterm-agent crate (async-xpty extracted to its own repo, #87).
allow = [
	"MIT",
	"Apache-2.0",
	"BSD-2-Clause",
	"ISC",
	"Unicode-3.0",
]
exceptions = [
	# Our own application crates — AGPL by decision (#78), never a dependency of the libs.
	{ name = "lasterm-agent", allow = ["AGPL-3.0-only"] },
	{ name = "lasterm-hub-lock", allow = ["AGPL-3.0-only"] },
	{ name = "lasterm-process-lock", allow = ["AGPL-3.0-only"] },
]
