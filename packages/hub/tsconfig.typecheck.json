{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"noEmit": true,
		"baseUrl": "../..",
		"paths": {
			"fastify": ["packages/hub/node_modules/fastify/fastify.d.ts"]
		}
	},
	// #206: keep this deliberately narrow until the 198 pre-existing strict test-code
	// diagnostics are fixed; widening to all src/** would make the branch gate red.
	"include": [
		"src/tls-identity.spec.ts",
		"src/**/*.fixture.ts",
		"src/**/*.setup.ts",
		"../../scripts/**/*"
	],
	"exclude": ["node_modules", "dist"]
}
